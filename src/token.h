#ifndef LIBPLTTAGGER_TOKEN_H
#define LIBPLTAGGER_TOKEN_H

#include <unicode/unistr.h>
#include <string>

namespace PlTagger {

	/**
	 * A single token being processed. The orth is stored as an ICU UnicodeString.
	 */
	class Token
	{
	};

} /* end ns PlTagger */

#endif // LIBPLTAGGER_TOKEN_H
